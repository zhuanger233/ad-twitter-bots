# Twitter/X Auto Subtitle Bot

?????? FastAPI + Celery + Redis + PostgreSQL ? Twitter/X ????????????????

- ?? bot ?? mentions
- ?????? tweet
- ??????????
- ????? ElevenLabs ? Whisper
- ?? SRT ??
- ?? ffmpeg ????
- ?????????? mention tweet
- ????????? PostgreSQL
- ?? Redis ?????????

## ????

?????

- `app/api`: FastAPI ??
- `app/core`: ???????????
- `app/db`: SQLAlchemy ?????
- `app/clients`: X?ElevenLabs?Redis??????
- `app/services`: ?????????
- `app/workers`: Celery ??
- `tests`: ?????????
- `alembic`: ?????

## ????

1. ????

```bash
pip install -r requirements.txt
```

2. ???? `.env`

???????? [`.env.example`](e:/WorkSpace/ad-twitter-bots/.env.example)???????????????

3. ????

```bash
alembic upgrade head
```

4. ?? API

```bash
uvicorn app.api.main:app --reload --host 0.0.0.0 --port 8000
```

5. ?? worker

```bash
celery -A app.workers.celery_app.celery_app worker -Q queue_mentions,queue_io,queue_asr,queue_ffmpeg,queue_post --loglevel=INFO
```

## API

- `GET /health`
- `POST /tasks/process`
- `GET /tasks/{task_id}`
- `GET /tasks`
- `POST /admin/poll-mentions`

?????

```json
{
  "mention_tweet_id": "1234567890",
  "video_tweet_id": "1234567000"
}
```

## X ??????

?? `XClient` ??????????

- mention tweet ??????????
- mention tweet ? reply??? tweet ???????? tweet
- mention tweet ? quote?? quoted tweet ??????? quoted tweet
- ??? media ? `variants` ??????? `video/mp4`
- ???? bot ????? mentions
- `X_BOT_USER_ID` ???????? `X_BOT_USERNAME` ?? bot user id

????? [x_client.py](e:/WorkSpace/ad-twitter-bots/app/clients/x_client.py)?

## Docker ???

?? [docker-compose.yml](e:/WorkSpace/ad-twitter-bots/docker-compose.yml) ???????? named volumes?

- Redis: `redis_data`
- PostgreSQL: `postgres_data`

????????????`docker compose down` ????????? Redis/PostgreSQL ?????????? volume ??????????

## ????

?????? [`bootstrap.sh`](e:/WorkSpace/ad-twitter-bots/bootstrap.sh)???? Ubuntu ????????

```bash
chmod +x bootstrap.sh
./bootstrap.sh
```

???????

- clone ??????? `$HOME/apps/ad-twitter-bots`
- ?? `.env` ????????????????????
- ????? `proxy_network`
- `docker compose up -d --build`
- ?? `alembic upgrade head`
- ??????

?????? [`deploy.sh`](e:/WorkSpace/ad-twitter-bots/deploy.sh)?

```bash
chmod +x deploy.sh
./deploy.sh
```

???????

- ??????
- ?? `.env` ???????????
- ????? `proxy_network`
- `docker compose up -d --build`
- ?? `alembic upgrade head`
- ????????

## ???????

??????????

- [`ops.sh`](e:/WorkSpace/ad-twitter-bots/ops.sh): ????????????????

?????

```bash
chmod +x ops.sh
./ops.sh stop
./ops.sh destroy
```

## ??

```bash
pytest
```

## ????

?????????????????? X v2 `includes.media` ? `referenced_tweets` ??????????????????????????????????????????? [x_client.py](e:/WorkSpace/ad-twitter-bots/app/clients/x_client.py) ???????????
